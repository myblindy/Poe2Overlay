<Window x:Class="Poe2Overlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Poe2Overlay"
        mc:Ignorable="d"
        x:Name="RootWindow"
        WindowStyle="None"
        Title="MainWindow" WindowState="Maximized" Topmost="True" Loaded="OnLoaded"
        AllowsTransparency="True" Background="{x:Null}">
    <Window.Resources>
        <local:PowerChargeToFillColorConverter x:Key="PowerChargeToFillColorConverter"/>
    </Window.Resources>
    <Canvas>
        <ItemsControl ItemsSource="{Binding ElementName=RootWindow, Path=PowerCharges}"
                      Canvas.Left="985" Canvas.Top="377">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle Width="8" Height="8" RadiusX="3" RadiusY="3" Margin="0,0,1,0"
                               Fill="{Binding Converter={StaticResource PowerChargeToFillColorConverter}}" 
                               StrokeThickness="1" Stroke="Black"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Canvas>
</Window>
